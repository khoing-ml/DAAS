# Core runtime dependencies
torch
torchvision
diffusers
transformers
Pillow
numpy
accelerate
safetensors

# Optional scorer integrations (install if you use corresponding scorers)
ImageReward @ git+https://github.com/zai-org/ImageReward.git    
hpsv2
