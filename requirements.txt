# Core runtime dependencies
torch
torchvision
diffusers
transformers==4.33.2
tokenizers>=0.14.0
Pillow
numpy
accelerate
safetensors

# Optional scorer integrations (install if you use corresponding scorers)
# ImageReward installed locally via setup_imagereward.sh
hpsv2
git+https://github.com/openai/CLIP.git
